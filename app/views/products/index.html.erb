<%= form_for :product, url: products_path do |f| %>
    <%= f.label "Product" %>
    <%= f.text_field :product %>

    <%= f.submit "add to cart" %>

<% end %>

<% cart.each do |product| %>
    <%= product %>
<% end %>
